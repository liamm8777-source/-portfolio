# Performance Guidelines

## Change Detection
- Use `OnPush` everywhere.
- Prefer signals over observables.

## Rendering
- Avoid complex templates.
- Use `trackBy` in all `*ngFor`.

## Bundles
- Lazy-load all features.
- Avoid large third-party libraries.

## Measurement
- Prefer real metrics over assumptions.
